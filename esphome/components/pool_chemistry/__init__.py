"""Pool chemistry helper library for ESPHome external components."""

# This component is a plain C++ library component. No code generation is required
# for the helper API itself. It is intended to be consumed by other components via
# an include such as `#include "pool_chemistry.h"`.
